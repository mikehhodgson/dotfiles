[user]
	name = Mike
	email = mavasplode@users.noreply.github.com
[core]
	autocrlf = input
	safectrl = true
	excludesfile = ~/.gitignore_global
[alias]
        co = checkout
        ci = commit
        st = status -sb
        br = branch
        hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
        type = cat-file -t
        dump = cat-file -p
[color]
	ui = true
[credential]
	helper = osxkeychain
[push]
	default = simple
