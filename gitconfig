[user]
	name = Mike Hodgson
	email = mikehhodgson@users.noreply.github.com
[core]
	autocrlf = input
	safectrl = true
	excludesfile = ~/.gitignore_global
[alias]
        co = checkout
        ci = commit
        st = status -sb
        br = branch
        hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --decorate --date=short --all
        type = cat-file -t
        dump = cat-file -p
	grog = log --graph --abbrev-commit --decorate --all --format=format:\"%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(dim white) - %an%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n %C(white)%s%C(reset)\"
[color]
	ui = true
[credential]
	helper = osxkeychain
[push]
	default = simple
